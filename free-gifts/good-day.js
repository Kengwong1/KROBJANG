// good-day.js
function findGoodDay() {
  const dateStr = document.getElementById('dateInput').value;
  if (!dateStr) {
    alert("กรุณาเลือกวันที่");
    return;
  }

  const date = new Date(dateStr);
  const day = date.getDay(); // 0=อาทิตย์, 1=จันทร์, ..., 6=เสาร์
  const month = date.getMonth() + 1;
  const year = date.getFullYear();

  // ตัวอย่าง: วันดีตามปฏิทินไทย (ไม่ใช่ระบบจริง — คุณสามารถปรับตามตำรา)
  const goodDays = [1, 3, 5, 7, 9]; // วันที่ดี (ตัวอย่าง)
  const badDays = [4, 6, 8]; // วันที่ควรระวัง

  let result = "";
  if (goodDays.includes(day)) {
    result = `<h3>✨ วันนี้เป็นวันดี! ✨</h3>
              <p><strong>วัน:</strong> ${getDayName(day)}<br>
              <strong>วันนี้เหมาะสำหรับ:</strong> แต่งงาน, เปิดร้าน, ทำบุญ, เริ่มงานใหม่</p>`;
  } else if (badDays.includes(day)) {
    result = `<h3>⚠️ วันนี้ควรระวัง ⚠️</h3>
              <p><strong>วัน:</strong> ${getDayName(day)}<br>
              <strong>คำแนะนำ:</strong> หลีกเลี่ยงงานใหญ่, ลงนามสัญญา, หรือเดินทางไกล</p>`;
  } else {
    result = `<h3>🌙 วันธรรมดา 🌙</h3>
              <p><strong>วัน:</strong> ${getDayName(day)}<br>
              <strong>คำแนะนำ:</strong> ทำงานปกติ, วางแผน, หรือพักผ่อน</p>`;
  }

  document.getElementById('goodDayResult').innerHTML = result;
}

function getDayName(day) {
  const days = ["อาทิตย์", "จันทร์", "อังคาร", "พุธ", "พฤหัสบดี", "ศุกร์", "เสาร์"];
  return days[day];
}